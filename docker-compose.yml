version: '3.8'

services:
  app:
    build: .
    ports:
      - "8081:8080"
    environment:
      - PORT=8080
      - WEATHER_API_KEY=${WEATHER_API_KEY}
    networks:
      - weather-network

networks:
  weather-network:
    driver: bridge
